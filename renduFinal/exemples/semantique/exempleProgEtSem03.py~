==> PROGRAMME
-------------------------

if("henri"=="max"):
 print "henri = max"
if("max"=="max"):
 print "max = max"

==> SEMANTIQUE
-------------------------

Programme(
	IC( 
		If( 
			Op_bin( Valeur( String( henri ) ), Equal, Valeur( String( max ) ) ),
			Programme(
				Print( Valeur( String( henri = max ) ) )
			)
		)
	)
	IC( 
		If( 
			Op_bin( Valeur( String( max ) ),Equal,Valeur( String( max ) ) ),
			Programme(
				Print( Valeur( String( max = max ) ) )
			)	
		)
	)
)

